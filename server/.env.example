# Copy to .env to override defaults.
PORT=4000
# Secret used to sign JWTs. Change this in production.
JWT_SECRET=dev-secret-change-me
# Token lifetime.
JWT_EXPIRES_IN=30d
# SQLite database file path (relative to server/).
DB_PATH=./data/alumni.db
